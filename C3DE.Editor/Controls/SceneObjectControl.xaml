<UserControl x:Class="C3DE.Editor.Controls.GameObjectControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="65" d:DesignWidth="250">
    <Border BorderThickness="1" BorderBrush="LightGray" Margin="2">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
            <Label Content="Scene Object" />
            <Separator Margin="5, 2, 5, 2" />
            <StackPanel Orientation="Horizontal" Margin="5 2 5 2">
                <CheckBox VerticalAlignment="Center" IsChecked="{Binding SceneObjectEnabled}" Margin="0 0 5 0"/>
                <TextBox Text="{Binding SceneObjectName}" Width="210" />
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
